---
description: Show the Fabrik3D sprint batch status, lock, human gate and next roadmap sprint
agent: build
---

Run `npm run sprint:batch:status` from the repository root and summarize it. This command is read-only: it must never activate, complete or modify a sprint, and it must never delete locks or gates.
